cmake_minimum_required(VERSION 2.8.12)
project( run_me )
find_package( OpenCV REQUIRED )
message("OpenCV version: ${OpenCV_VERSION}")
add_executable( run_me current_file.cpp )
target_link_libraries( run_me ${OpenCV_LIBS} )